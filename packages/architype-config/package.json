{
  "name": "@vnx-architype/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration for VNX-Architype",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linting configured for config package'",
    "build": "echo 'No build step for config package'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

